# Optional dependencies for enhanced features
# These may require compilation and can be skipped for basic functionality

# Advanced data processing (requires compilation)
pandas==2.1.4
numpy==1.25.2

# Barcode generation with images (requires Pillow compilation)
python-barcode[images]==0.15.1

# PDF generation for receipts/reports (may require compilation)
reportlab==4.0.7

# Excel file handling
openpyxl==3.1.2

# Database migrations (optional)
alembic==1.12.1

# Enhanced authentication (requires cryptography compilation)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
